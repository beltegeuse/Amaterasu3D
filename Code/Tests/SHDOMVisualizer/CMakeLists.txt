SET(SHDOMVisualizer_SRCS
	main.cpp
)


INCLUDE_DIRECTORIES(
	${CMAKE_CURRENT_SOURCE_DIR}
	../../Libs/Amaterasu3D
)

ADD_EXECUTABLE(SHDOMVisualizer ${SHDOMVisualizer_SRCS})

target_link_libraries(
  SHDOMVisualizer
  Amaterasu3D
)

SET_TARGET_PROPERTIES(SHDOMVisualizer PROPERTIES
  INSTALL_RPATH ${PROJECT_BUILD_PATH}
  INSTALL_RPATH_USE_LINK_PATH true
)

install(TARGETS SHDOMVisualizer RUNTIME DESTINATION ${PROJECT_BUILD_PATH})