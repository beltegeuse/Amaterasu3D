PROJECT(Amaterasu3D)

#------- Configuration ------------
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
SET(CMAKE_COLOR_MAKEFILE ON)
SET(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/Config/CMake ${CMAKE_MODULE_PATH})

# * -- To install project ---
SET(PROJECT_BUILD_PATH ${CMAKE_SOURCE_DIR}/Build)

# * --Subprojects--
# *** SFML
SET(SFMLDIR C:/msys/1.0/build/SFML-2)
#FIND_PACKAGE(SFML REQUIRED)
FIND_PACKAGE(GLEW REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)
FIND_PACKAGE(Freetype REQUIRED)
FIND_PACKAGE(FreeImage REQUIRED)
FIND_PACKAGE(ZLIB REQUIRED)
FIND_PACKAGE(SDL REQUIRED)
#FIND_PACKAGE(SigCXX2 REQUIRED)

# *** Boost
IF(WIN32)
set(BOOST_ROOT C:/MinGW)
ELSE()
set(BOOST_ROOT /usr)
ENDIF()
set(BOOST_LIBS unit_test_framework filesystem system regex) # LINUX PROBLEM : random
find_package(Boost COMPONENTS ${BOOST_LIBS} REQUIRED)
MESSAGE("Boost libraries link is ${Boost_LIBRARIES}")

ADD_SUBDIRECTORY(Code)
